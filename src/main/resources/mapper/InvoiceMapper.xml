<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.hospital.mapper.IInvoiceMapper">
    <insert id="insertInvovice" parameterType="Invoice">
        INSERT INTO invoice VALUES (null,#{number},#{payAmount},#{status},null,#{userId},#{registerId},#{payWay})
    </insert>
</mapper>